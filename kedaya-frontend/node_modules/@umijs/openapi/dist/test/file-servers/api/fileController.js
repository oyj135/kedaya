"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.importNavReCheckFileUsingPost = void 0;
const tslib_1 = require("tslib");
// @ts-ignore
/* eslint-disable */
const umi_1 = require("umi");
/** 导入文件 导入文件 POST /webapi/clear/importNavReCheckFile */
function importNavReCheckFileUsingPost(body, file, options) {
    return tslib_1.__awaiter(this, void 0, void 0, function* () {
        const formData = new FormData();
        if (file) {
            formData.append('file', file);
        }
        Object.keys(body).forEach((ele) => {
            const item = body[ele];
            if (item !== undefined && item !== null) {
                if (typeof item === 'object' && !(item instanceof File)) {
                    if (item instanceof Array) {
                        item.forEach((f) => formData.append(ele, f || ''));
                    }
                    else {
                        formData.append(ele, JSON.stringify(item));
                    }
                }
                else {
                    formData.append(ele, item);
                }
            }
        });
        return umi_1.request('/webapi/clear/importNavReCheckFile', Object.assign({ method: 'POST', data: formData, requestType: 'form' }, (options || {})));
    });
}
exports.importNavReCheckFileUsingPost = importNavReCheckFileUsingPost;
